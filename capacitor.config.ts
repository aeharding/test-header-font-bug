import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'test-header-font-bug',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
